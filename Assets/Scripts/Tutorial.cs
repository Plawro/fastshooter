using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tutorial : MonoBehaviour
{
    // X to hide labels (all at once)
    // C to hide tutorial texts (per camera)
    // Tutorial stuff
    // Intro and outro management
    [SerializeField] GameObject[] towerLabels;
    [SerializeField] GameObject[] powerPlantLabels;
    [SerializeField] GameObject[] hallwayLabels;
    [SerializeField] GameObject[] controlPanelLabels;
    [SerializeField] GameObject[] outsideLabels;
}
